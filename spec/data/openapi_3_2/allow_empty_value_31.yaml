openapi: 3.1.0
info:
  title: Pet API
  version: '1.0'
paths:
  /pets:
    get:
      summary: List pets
      parameters:
        - name: q
          in: query
          allowEmptyValue: true
          schema:
            type: string
      responses:
        '200':
          description: OK
