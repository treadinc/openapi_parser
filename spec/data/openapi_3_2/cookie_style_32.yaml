openapi: 3.2.0
info:
  title: Pet API
  version: '1.0'
paths:
  /pets:
    get:
      summary: List pets
      parameters:
        - name: session
          in: cookie
          style: cookie
          schema:
            type: string
      responses:
        '200':
          description: OK
