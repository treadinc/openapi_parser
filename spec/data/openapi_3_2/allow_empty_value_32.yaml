openapi: 3.2.0
info:
  title: Pet API
  version: '1.0'
paths:
  /pets:
    get:
      summary: List pets
      parameters:
        # `allowEmptyValue` on a Parameter Object is deprecated in 3.2, so its
        # use here is a spec violation.
        - name: q
          in: query
          allowEmptyValue: true
          schema:
            type: string
      responses:
        '200':
          description: OK
